import { BaseDTO } from '../../BaseDTO';

export class MetadataList extends BaseDTO
   {
       MetadataListId: number;
       MetadataListName: string;
       status: number;
       StatusName: string;
}